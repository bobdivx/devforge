# Application DevForge

Application Astro avec Preact, Tailwind CSS, DaisyUI et SQLite.

## Développement

```bash
npm install
npm run dev
```

## Production

```bash
npm run build
npm start
```

L'application écoute sur le port défini par la variable `PORT` (par défaut 3000).
